import { SubShell } from "../_components/SubShell";

export default function SettingsNotifications() {
  return (
    <SubShell
      title="Notifications"
      body="Email digest cadence, push notifications, imminent-shoot reminders, and payout alerts. Push needs a one-time browser permission grant."
    />
  );
}
